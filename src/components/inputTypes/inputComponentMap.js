import ExponentInput from './exponentInput';
// import other types here in the future

const inputComponentMap = {
  exponentInput: ExponentInput,
  // fractionInput: FractionInput,
  // etc.
};

export default inputComponentMap;